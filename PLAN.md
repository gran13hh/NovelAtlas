# NovelAtlas 分阶段实施计划

本文档用于把从零构建 NovelAtlas 的过程拆分为可独立运行、独立验证的小阶段，避免一次改动过大导致问题难以定位。

## 协作方式

每个阶段遵循同一流程：

1. Codex 只实现当前阶段约定的范围，并运行自动检查。
2. Codex 给出该阶段的手动启动与验收步骤。
3. 用户在本机和 PyCharm 中进行验证。
4. 验收失败时只修复当前阶段，不提前开发下一阶段。
5. 用户确认通过后，将阶段状态改为“已验收”，再进入下一阶段。

状态说明：

- `未开始`：尚未实现。
- `开发中`：正在实现，不能进入下一阶段。
- `待验收`：代码与自动检查已完成，等待用户手动验证。
- `已验收`：用户确认通过，可以继续。

## 关键技术注意事项

以下问题暂不在当前阶段深入实现，但后续设计和开发必须持续考虑，避免在功能完成后再进行大规模返工。

### 1. TXT 章节识别与内容拆分

小说 TXT 通常需要使用正则表达式识别卷名、章节标题和正文边界，但不能假设所有小说都采用相同格式。后续实现时需要注意：

- 兼容“第 X 章”“第 X 回”“卷一”“正文 第 X 章”等常见标题形式。
- 避免将正文中偶然出现的相似短句误判为章节标题。
- 正则规则需要可配置、可组合，并提供无章节标题时的降级分块策略。
- 拆分过程中必须保留原文字符区间、章节编号和稳定引用 ID，保证分析结论能够定位回原文。
- 使用多种真实格式的短篇测试样例建立章节识别回归测试。

该问题主要在阶段 2 的文件解析和阶段 3 的章节识别、分块与引用定位中处理。

#### 首版解析已知问题（初步功能完成后集中 Debug）

阶段 3 先建立可运行的通用解析基线，不在此时针对单本小说不断追加特例。以下风险已记录，待阶段 8 初步交付前使用更多真实小说样本统一修正并建立回归测试：

- **卷与章的层级关系**：例如“第1卷”后紧接“第一章 凝真”时，“第1卷”本质上是包含多章的层级容器，不应被当成与章节平级的正文单元。当前实现会把卷标题建立为独立解析项，其内容可能只有换行等空白，进而出现空预览或仅包含空白 Token 的文本块。
- **结构标题不应进入正文分块**：卷名、篇名等结构节点是否需要独立引用、是否计入 Token，以及它们与下属章节的坐标关系，需要在引入“卷 → 章 → 文本块”层级模型时统一确定。
- **章节编号格式混用**：同一本小说可能交替使用“第一章”“第1章”“第１２章”“一、”“Chapter 1”等形式，还可能存在跳号、重复号、番外编号和 OCR 错字。当前规则支持中文数字、阿拉伯数字和全角数字等部分形式，但尚未验证编号连续性和全书一致性。
- **正文误判为标题**：正文、引用内容或功法秘籍中可能出现“长春功第三章”“第三章 修炼要诀”等类似章节的文字；若其排版恰好独占一行，单靠正则表达式仍可能误判。后续需要结合上下文、相邻标题规律、标题长度和编号序列进行二次判定。
- **真实 TXT 排版差异**：连续空行、全角空格、不可见字符、标题与正文同行、缺失换行和异常标点都可能造成漏识别或边界偏移，需要通过真实样本逐项补充测试。

在集中 Debug 前，上述行为作为已知限制保留；后续 Agent 和引用功能仍须只依赖稳定字符区间，避免把当前章节层级假设固化到所有模块。

### 2. 数百万字长篇小说的大模型输入策略

长篇小说可能达到数百万字，不能将全文一次性提交给大模型。后续需要专门设计长文本处理策略，初步约束如下：

- 按模型 Token 而不是简单字符数控制每次请求大小，并为提示词和输出预留空间。
- 采用“相邻章节动态装箱 → 批次重要内容概括 → 批次结果分层汇总 → 全书细纲”的流程。
- 优先保持完整章节；只有单章超过输入预算时才按文本块拆分，并去除相邻重叠内容。
- 每个成功批次立即保存结构化结果、输入指纹和完成状态，避免重复提交原文。
- 全书汇总只处理批次概括；内容仍超限时继续分层归并，而不是一次提交全部摘要。
- 对人物、关系、地点、事件和伏笔进行跨批次归并，对远距离疑似关联明确标记不确定性。
- 评估概括造成的信息丢失、摘要漂移和前后矛盾，建立固定长文本测试样例。
- 记录 Token 用量、调用次数、并发、耗时和费用，为用户提供可预期的处理策略。
- 考虑失败重试、断点恢复与结果缓存，避免单个步骤失败后重新处理整本小说。

该问题主要在阶段 3 的分块设计、阶段 4 的模型网关和阶段 5 的 LangGraph 工作流中处理。在确认具体模型及上下文窗口前，不锁定最终算法。

### 3. 解析结果的浏览器端临时保存

用户希望小说解析完成后的结构化结果可在浏览器端临时保存。该内容不同于阶段 2 的小说原文临时文件，后续需要根据结果体积、刷新恢复、隐私和清理要求，在内存、`sessionStorage` 与 IndexedDB 之间选择。分析结果缓存不能混入模型密钥，也不能默认成为需要长期可靠保存的唯一副本。阶段 4 根据新增需求，允许用户明确选择将模型 API 配置（包括 Key）单独保存在当前站点的 `localStorage`；该配置必须使用独立存储键、显示明文缓存风险并提供立即清除入口。

该问题主要在阶段 6 的分析工作台与人工修正中处理，阶段 2 不将小说正文写入浏览器持久缓存。

## 需求调整基线：从“小说图谱”收缩为“全书细纲”

2026-09-02 根据实际阅读需求调整产品范围。NovelAtlas 首版不再尝试自动完成文风鉴赏、精彩原句摘录、人物或地图生图、风格仿写，而是聚焦为：输入一本 TXT 小说，返回一份按章节范围可回看的、稍微细致的全书大纲。

### 保留的已验收能力

- Git、项目骨架、一键启动和前后端健康检查。
- TXT 上传、编码识别、临时任务目录、删除与过期清理。
- 章节识别、Token 分块、稳定 ID、文本块编辑与删除。
- 文本模型 Base URL、API Key、模型列表、连接测试、Mock、超时、重试和脱敏。
- LangGraph、Pydantic、SSE 等基础依赖。

### 将删除的遗留能力

- 图片模型环境变量、Provider、Gateway 分支、请求/响应 Schema、Mock、OpenAI Image 适配器和测试路由。
- 前端图片模型配置、图片模型选择和图片调用测试。
- 原阶段 5 的文风 Agent、剧情/人物/世界观独立二次 Agent，以及只消费章节摘要的流程。
- 原阶段 5 的章节摘要、实体归并、引用校验 Skills 及其结果 Schema；其中可复用思想可在新 Skill 中重新实现，但不保留不匹配的数据流。
- README、PLAN 和界面中生图、仿写、好句摘录、专项描写分析的当前功能描述。

### 将保留并重构的阶段 5 基础

- 后台任务与 SSE 订阅解耦，浏览器断开不取消分析。
- API Key 只在内存中随调用使用，不写入 Checkpoint、SSE、结果和日志。
- 结构化输出经过 Pydantic 校验。
- 删除或过期上传任务时同步清理分析状态。
- 现有分析路由路径可以保留，但请求、状态、结果和重试语义改为批次级。

### 新的首版输出

- 全书总体概述。
- 按章节范围排列的批次概括和剧情细纲。
- 主要故事线、关键事件与转折。
- 主要人物、人物关系及阶段性变化。
- 主要世界观：地点、势力、规则、功法、物品和概念。
- 伏笔、未解决事件、疑似远距离关联和冲突；不确定内容必须明确标记。

### 暂不开发

- 文风、好句与原文摘录。
- 服装、外貌、景物、战斗等专项描写分析。
- 图片生成、地图生成和人物印象图。
- 风格仿写。
- 连载续写只作为 README 中的未来可能性，不创建页面、API、Schema 或占位开关。

## 阶段 0：项目与环境初始化

**状态：已验收**

范围：

- 初始化 Git 仓库并建立 `main` 分支。
- 创建项目 README、`.gitignore` 和 Python 依赖清单。
- 创建项目本地 `.venv`，安装 FastAPI、LangGraph 等基础依赖。
- 创建 React + TypeScript + Vite 前端并安装基础 npm 依赖。

验收标准：

- Git 仓库可用。
- `.venv/bin/python` 指向项目本地环境。
- `pip check` 无依赖冲突。
- 前端依赖已安装。

## 阶段 1：前后端最小可运行骨架

**状态：已验收**

范围：

- 创建 FastAPI 应用入口和 `/api/health` 健康检查。
- 创建健康检查自动测试。
- 配置 Vite 将 `/api` 请求代理到本地 FastAPI。
- 将 Vite 示例页替换为 NovelAtlas 最小产品首页。
- 前端读取健康检查并展示连接状态。

不包含：

- TXT 上传和解析。
- 真实模型调用。
- Agent 工作流。
- 数据导出。

验收标准：

- 后端测试通过。
- 前端 lint 和生产构建通过。
- 打开前端能够看到 NovelAtlas 首页。
- 后端运行时，页面显示“API 已连接”。
- 停止后端时，页面显示清晰的未连接状态，前端本身不崩溃。

手动验收步骤：

1. 推荐双击项目根目录的 `start.command`，它会同时启动前后端并自动打开浏览器。在 PyCharm 中也可以直接运行 `scripts/dev.py`。
2. 如果需要分别启动，在项目根目录启动后端：

   ```bash
   source .venv/bin/activate
   python -m uvicorn apps.api.novelatlas_api.main:app --reload --port 8000
   ```

3. 打开第二个终端启动前端：

   ```bash
   cd apps/web
   npm run dev
   ```

3. 打开终端显示的本地前端地址（通常是 `http://localhost:5173`）。
4. 确认页面右上角显示“API 已连接 · v0.1.0”。
5. 打开 `http://127.0.0.1:8000/api/health`，确认返回 `status: ok`。
6. 停止后端，等待最多约 30 秒，确认前端切换为“API 未启动”且页面不崩溃。
7. 可选地再次运行自动检查：

   ```bash
   python -m pytest apps/api/tests -q
   python -m ruff check apps/api
   cd apps/web && npm run lint && npm run build
   ```

## 阶段 2：TXT 上传与临时文件生命周期

**状态：已验收**

范围：

- 增加 TXT 文件选择、拖放和上传界面。
- 后端校验扩展名、MIME、文件大小和空文件。
- 检测 UTF-8、GB18030 等常见编码并规范化文本。
- 为每次上传创建临时任务目录和随机任务 ID。
- 实现任务取消、删除和超时清理。
- 不建立用户数据库，不长期保存小说正文。

验收标准：

- 合法 TXT 可上传并返回元数据。
- 非 TXT、空文件、超限文件得到明确错误。
- 中文编码样例能够正确读取。
- 删除任务后临时文件确实消失。

手动验收步骤：

1. 在项目根目录启动后端：

   ```bash
   source .venv/bin/activate
   python -m uvicorn apps.api.novelatlas_api.main:app --reload --port 8000
   ```

2. 打开第二个终端启动前端：

   ```bash
   cd apps/web
   npm run dev
   ```

4. 打开前端地址，将 `tests/fixtures/novels/sample_utf8.txt` 拖入上传区域。
5. 确认页面显示文件名、大小、字符数、UTF-8 编码、任务 ID 和自动清理时间。
6. 点击“立即删除”，确认上传卡片恢复为文件选择区域。
7. 尝试选择非 TXT、空 TXT 和超过 50 MiB 的文件，确认页面给出明确错误。
8. 可在 `http://127.0.0.1:8000/docs` 中直接测试上传、查询和删除接口。
9. 可选地用短过期时间启动后端，验证自动清理：

   ```bash
   NOVELATLAS_UPLOAD_TTL_SECONDS=10 \
   NOVELATLAS_CLEANUP_INTERVAL_SECONDS=2 \
   python -m uvicorn apps.api.novelatlas_api.main:app --port 8000
   ```

10. 可选地再次运行自动检查：

   ```bash
   python -m pytest apps/api/tests -q
   python -m ruff check apps/api novelatlas
   cd apps/web && npm run lint && npm run build
   ```

## 阶段 3：章节识别、分块与引用定位

**状态：已验收（含文本块编辑与删除）**

范围：

- 建立小说文档、章节、文本块和引用位置 Schema。
- 识别常见中文章节标题。
- 对超长章节进行有重叠的安全分块。
- 保存章节号、字符区间和稳定引用 ID。
- 提供解析预览接口和前端章节列表。
- 解析完成后可按需读取、编辑和删除当前任务中的文本块；编辑不改写原始 TXT 与引用坐标，重新解析可恢复原始分块。

验收标准：

- 固定测试小说能够稳定得到相同章节与分块。
- 任意分析结论可定位回对应文本区间。
- 无章节标题的文本也能降级处理。

已实现说明：

- 章节标题按独立行识别，首版支持“第 X 章/节/回/部/篇/集”“第 X 卷/卷 X/上中下卷”以及序章、楔子、尾声等特殊标题。
- 带句末标点的正文行不会被识别为标题；规则以可组合列表实现，后续可根据真实小说样例继续扩展。
- 无标题时建立“全文”降级章节；首个标题之前存在正文时建立“卷首”章节，避免丢失前言内容。
- 使用 `o200k_base` 进行确定性 Token 计数，默认每块最多 6000 Token、相邻块重叠最多 300 Token，均可通过环境变量调整。
- 每个章节、文本块和引用都包含稳定 ID；引用采用规范化原文中的半开字符区间 `[start, end)`，可以直接定位回 `source.txt`。
- 解析清单保存在上传任务临时目录的 `parse-result.json`，会随用户删除或任务过期一起清理，浏览器暂不持久化正文。
- 浏览器只在用户打开某个文本块时读取该块正文，避免将整本小说及重叠块一次性复制到浏览器内存。
- 编辑以 `content_override` 形式保存在当前解析清单中，原始 `source.txt`、引用 ID 和字符区间保持不变；编辑后的内容仍受单块 Token 上限约束。
- 删除会从当前解析清单及所属章节的块列表中移除目标块，但不删除原始 TXT；重新解析会清除编辑覆盖并恢复所有原始文本块。
- 当前编辑只影响所选文本块，不联动修改相邻的重叠文本块；该限制会与其他长文本边界问题一起在初步功能完成后集中 Debug。

手动验收步骤：

1. 使用项目根目录的 `start.command` 启动，或按阶段 2 的方式分别启动前后端。
2. 上传 `tests/fixtures/novels/sample_utf8.txt`，点击“开始解析章节”。
3. 确认页面显示“2 个章节 · 2 个文本块”，章节名称为“第一章 初见”和“第二章 来信”。
4. 展开章节，确认能够看到正文预览、Token 数和形如 `[start, end)` 的原文区间。
5. 点击“重新解析”，确认章节数量、区间与 ID 保持不变。
6. 上传 `tests/fixtures/novels/sample_no_headings.txt`，确认解析结果显示“无标题降级”，章节名为“全文”。
7. 点击“立即删除”，确认上传信息与解析预览一起消失。
8. 可选地再次运行自动检查（这些命令不会启动服务）：

   ```bash
   python -m pytest apps/api/tests -q
   python -m ruff check apps/api novelatlas
   cd apps/web && npm run lint && npm run build
   ```

补充优化验收步骤：

1. 完成解析后展开任意章节，确认正文预览下方显示该章的文本块列表和 Token、原文区间。
2. 点击“编辑或删除”，确认只在此时加载并显示该文本块正文。
3. 修改正文并保存，确认文本块出现“已编辑”标记；再次打开时仍显示修改后的内容。
4. 点击“恢复原文”并保存，确认“已编辑”标记消失。
5. 再次打开编辑器并点击“删除文本块”，确认后选择删除，检查章节文本块数量和全书文本块总数相应减少。
6. 点击“重新解析”，确认修改和删除均被撤销，原始文本块恢复。
7. 确认页面提示编辑不会改写原始 TXT、引用坐标或相邻重叠块。

## 阶段 4：文本模型网关与模拟模型

**状态：已完成（2026-09-02 已验收）**

范围：

- 定义独立的文本模型接口。
- 通过环境变量配置供应商、模型、Base URL 和密钥。
- 提供浏览器配置界面，缓存文本模型的 Base URL、API Key 与模型选择。
- 实现不需要密钥的 Mock 文本模型，供完整流程测试。
- 增加超时、重试、错误归一化和敏感日志过滤。
- 只接入用户选定的第一种真实模型供应商。

验收标准：

- 不配置真实密钥也能通过 Mock 模型运行测试。
- 切换供应商不需要修改 Agent 业务代码。
- 前端不会接触或显示服务端 `.env` 密钥；用户主动输入的浏览器 Key 只在当前页面与独立本地缓存中使用。

已实现说明：

- 已验收提交曾同时包含 `TextModelProvider` 与后来取消的 `ImageModelProvider`；阶段 5A 已删除图片分支，只保留 Agent 依赖的文本 `ModelGateway`。
- 文本通过 `NOVELATLAS_TEXT_MODEL_*` 环境变量选择供应商、模型、Base URL 和 API Key；旧 `NOVELATLAS_IMAGE_MODEL_*` 已删除。
- 文本能力默认使用确定性 Mock；Mock 不联网、不需要密钥、不回显用户输入。
- 首个真实供应商适配器为 OpenAI Responses API 兼容文本接口，并设置 `store: false`。
- 对连接超时、网络失败、认证、权限、限流、服务端错误、无效 JSON 和空结果进行统一错误归一化；仅对网络类、限流和服务端错误执行有上限的指数退避重试。
- API Key 使用服务端 Secret 配置，公开状态接口只返回供应商、模型、Base URL、是否就绪及缺少的环境变量名；包含凭据、查询参数或片段的 Base URL 会被拒绝且不会返回浏览器。
- 首页文本模型配置可填写 Base URL、API Key 和模型；通过兼容接口的 `GET /models` 测试连接并读取该 Key 可见的模型，也允许手动输入模型 ID。图片配置界面已删除。
- 浏览器配置以版本化独立键保存在当前站点的 `localStorage`，不会改写 `.env`、后端文件或进程默认网关；Key 仅随用户发起的模型操作临时发送给后端，成功和错误响应均不回传 Key。
- 界面明确提示浏览器明文缓存风险，支持显示/隐藏 Key、未保存修改提示和一键清除配置；远程文本测试属于显式用户操作并提示可能产生费用。

手动验收步骤：

1. 不配置任何模型环境变量，启动项目并找到首页“Stage 04 · Model Gateway”区域。
2. 确认文本配置默认是本地 Mock；分别点击“测试连接并读取模型”和文本调用测试，确认均成功。
3. 将文本接口改为“OpenAI / 兼容接口”，确认可以填写 Base URL、API Key，并选择或手动输入模型。
4. 点击“保存到此浏览器（含 Key）”，刷新页面，确认设置能够恢复；在开发者工具的 localStorage 中可以看到这份明文配置，符合界面风险提示。
5. 点击“清除浏览器配置”，刷新页面，确认恢复 Mock 默认值且之前的 Key 不再存在。
6. 打开 `http://127.0.0.1:8000/api/models/config`，确认它仍只显示服务端默认配置且没有浏览器 API Key。
7. 可选：使用愿意产生调用费用的有效兼容 API 配置，点击“测试连接并读取模型”，从返回列表选择模型后再执行文本测试。
8. 可选地再次运行自动检查（这些命令不会启动服务）：

   ```bash
   python -m pytest apps/api/tests -q
   python -m ruff check apps/api novelatlas scripts
   cd apps/web && npm run lint && npm run build
   ```

## 阶段 5：动态批次概括、持久恢复与全书细纲

**状态：已验收**

范围：

- 根据模型 Token 预算，把连续短章节动态组合为原文批次。
- 每个批次只调用一次概括 Agent，同时提取关键事件、人物与关系变化、世界观线索、伏笔和未解决事项。
- 每批成功后立即持久化结果和状态；失败后从该批次重试，不重复已完成批次。
- 将全部批次概括按 Token 预算进行一次或多层汇总，生成全书细纲。
- 使用结构化 Schema 校验批次概括、汇总中间层和最终细纲。
- 通过 SSE 输出批次规划、当前批次、汇总层级、进度和错误。

验收标准：

- Mock 模型可从批次规划完整跑到最终细纲。
- 多个短章节能在 Token 预算内合并为一次请求；超长章节能安全拆分。
- 已完成批次不会因后续网络错误而重复调用；后端重启后可从临时清单恢复进度。
- 浏览器 API Key 不落盘；重启后恢复远程任务时由浏览器重新提供。
- 最终细纲包含总体概述、章节范围细纲、故事线、主要人物关系、世界观、伏笔和不确定项。
- SSE 断开不会导致分析任务崩溃。

旧实现记录（已判定不符合需求，以下验收步骤作废）：

- 使用 LangGraph `StateGraph` 建立 `prepare → chapter_summary → plot_agent → character_agent → style_agent → worldview_agent → citation_validation` 显式节点链，并用进程内 Checkpointer 保存节点级状态；图状态只保存章节摘要和结构化结果，不保存整本原文。
- 章节摘要 Skill 逐个读取解析阶段产生的有界文本块，经过模型调用和 Pydantic 校验后再按章节归并；四个专门 Agent 只消费摘要，并在摘要过长时按批次运行，避免把数百万字小说一次性提交给模型。
- 在 `novelatlas/skills/` 下建立章节摘要、实体归并、引用校验三个独立 Skill 目录，每个目录同时包含实现与 `SKILL.md`；人物和世界观实体只在规范名或明确别名相交时保守合并。
- 剧情、人物、文风、世界观分别使用独立结构化 Schema。真实模型返回的 JSON 必须通过 Pydantic 校验；Mock 模型生成确定性结构化结果，便于不联网验收完整流程。
- 所有分析 Claim 必须具有解析器签发的引用 ID，或者明确标为 `uncertain` 并说明原因；最终引用校验会移除未知 ID、记录无效 ID，并把失去有效证据的结论降级为不确定。
- 新增后台分析任务接口：`POST /api/analyses/{task_id}` 启动、`GET /api/analyses/{task_id}` 查询结果、`POST /api/analyses/{task_id}/retry` 从失败节点重试、`GET /api/analyses/{task_id}/events` 订阅 SSE。
- SSE 订阅者与后台任务生命周期解耦，断开或关闭订阅不会取消 LangGraph；SSE 支持历史事件回放与 `Last-Event-ID`。单节点失败会返回失败节点、安全错误和可重试状态，重试从最近成功 Checkpoint 继续，不重复已完成节点。
- 分析结果作为 `analysis-result.json` 保存在对应上传任务临时目录；删除或过期清理上传任务时，会同步取消后台工作流并清除内存状态和全部分析产物。
- 分析启动接口既可使用服务端文本模型配置，也可接收一次性的浏览器文本模型配置；API Key 不进入任务状态、结果、SSE 或临时产物。

旧验收步骤（已作废，仅用于说明为什么需要重构）：

1. 启动项目，上传并解析一份小型 TXT，复制页面或上传响应中的 32 位 `task_id`。
2. 打开 `http://127.0.0.1:8000/docs`，调用 `POST /api/analyses/{task_id}`，请求体填写 `{}`；确认返回 `202`、状态为 `queued` 或 `running`，默认供应商为 Mock。
3. 调用 `GET /api/analyses/{task_id}`，直至状态变为 `completed`；确认进度为 100，并能看到章节摘要、剧情、人物、文风和世界观五部分结构化结果。
4. 检查结果内的关键 Claim：有证据的结论包含 `cite_...` 引用 ID，没有证据的结论必须是 `confidence: uncertain` 且带有 `uncertainty_reason`。
5. 在终端执行 `curl -N http://127.0.0.1:8000/api/analyses/<task_id>/events`，确认可以看到阶段开始、完成和最终完成事件；中途按 `Ctrl+C` 断开后再次查询任务，任务仍应继续或已经完成。
6. 再次启动同一任务可重新分析；分析运行期间重复启动应返回 `409`。只有任务失败时 `POST /api/analyses/{task_id}/retry` 才可用，正常任务调用会返回 `409`。
7. 删除该上传任务后，再查询对应分析应返回 `404`，临时目录中的 `analysis-result.json` 也应随任务一起删除。
8. 可选地运行自动检查（这些命令不会启动服务）：

   ```bash
   python -m pytest apps/api/tests -q
   python -m ruff check apps/api novelatlas scripts
   cd apps/web && npm run lint && npm run build
   ```

### 阶段 5 重构执行顺序

#### 5A. 清理需求外代码

**状态：已完成（并入阶段 5 统一验收）**

- 删除图片模型后端、环境变量、Schema、路由、测试和前端配置。
- 删除旧剧情、人物、文风、世界观 Agent 和旧 Skills。
- 删除旧分析结果 Schema、后台任务和 SSE；后续按批次持久恢复需求重新实现，避免沿用错误的数据模型。
- 全量测试确认阶段 0～4 的上传、解析、编辑和文本模型功能未回归。

实现记录：

- 已删除图片模型配置、Provider、Schema、路由、测试及前端表单，只保留文本模型网关。
- 已删除未提交的旧四类分析 Agent、旧 Skills、旧分析任务接口和结果 Schema。
- 首页已改为批次规划、逐批概括、分层汇总和详细大纲的产品表述。
- 用户提供的 `tests/fixtures/novels/test_novel.txt` 保持原样，留作 5B 动态组批及后续端到端测试。
- 自动回归：后端 `34 passed`，Ruff、前端 Lint、前端生产构建、`pip check` 和 `git diff --check` 均通过。

#### 5B. Token 预算与动态批次规划

**状态：已完成（并入阶段 5 统一验收）**

- 文本模型配置增加上下文窗口、最大分析输入、预留输出和安全余量。
- 生成无重叠的分析片段；优先按完整章节装箱，超长章节才拆分。
- 为每批生成稳定 `batch_id`、章节范围、文本块列表、Token 数和输入指纹。
- 开始付费分析前返回批次数、预计概括调用数、预计汇总调用数和输入 Token。

实现记录：

- 服务端和浏览器模型配置均增加上下文窗口、最大输入、输出预留及安全余量；浏览器缓存从版本 1 平滑迁移到版本 2。
- 新规划器将解析阶段的滑动重叠块重组为无重复正文；完整短章节优先相邻装箱，单章超过预算时才生成无重叠片段。
- 每个片段和批次均包含稳定 ID、内容 SHA-256 指纹、章节范围、来源文本块、Token 数及编辑状态，但规划 API 不返回正文。
- 新增 `GET /api/analyses/config`、`POST /api/analyses/{task_id}/plan` 和 `GET /api/analyses/{task_id}/plan`；计划原子写入任务临时目录。
- 编辑、删除文本块或重新解析会自动作废旧计划，防止后续使用与当前正文不一致的批次。
- 首页可在不调用模型的情况下展示批次、片段、概括调用、汇总调用和预计输入 Token；真实中文样本已加入规划回归测试。
- 自动回归：后端 `42 passed`，Ruff、`pip check`、前端 Lint 与生产构建均通过；未调用真实模型。

#### 5C. 批次概括 Agent 与持久 Checkpoint

**状态：已完成（并入阶段 5 统一验收）**

- 一个原文批次对应一次模型请求。
- 输出批次概述、关键事件、人物、关系变化、世界观线索、伏笔和未解决事项；缺少的类别返回空列表。
- 每批通过 Pydantic 校验后立即写入任务临时目录，并更新原子化任务清单。
- 失败状态精确到批次；重试失败批次后继续下一批，不重新调用已完成批次。
- 服务器重启后从任务清单恢复；远程模型 Key 由浏览器重新提交，不写入磁盘。

实现记录：

- 新建单一职责的 `BatchSummaryAgent` 和 `batch_summary` Skill；一个批次严格对应一次逻辑模型调用，正文中的指令被明确视为不可信小说内容。
- 批次输出包含概述、关键事件、人物与关系变化、世界观线索、伏笔和未解决事项；空类别返回空列表，不再包含文风、摘句、生图或仿写内容。
- 真实模型输出必须通过 Pydantic 结构校验，并且所有章节 ID、文本块 ID 都必须属于当前批次；Mock 仍执行一次网关调用，再生成确定性结构化结果。
- 每个批次结果先原子写入独立 JSON，再更新 `analysis-task.json` 清单；如果进程恰好在两次写入之间中断，重启后会识别已有结果并补全清单，不重复调用。
- 失败状态精确记录批次、逻辑尝试次数、安全错误和是否可重试；再次调用同一 `run` 接口会跳过有效的已完成结果，从失败或中断批次继续。
- 浏览器模型配置只在后台任务内存中使用；任务清单和批次结果只记录供应商与模型名，不保存 API Key、Base URL 或批次原文。
- 默认临时根目录改为跨后端进程保留、受 TTL 管理的操作系统临时目录；应用重建后可读取同一任务清单与批次结果。
- 新增 `POST/GET /api/analyses/{task_id}/run` 和 `GET /api/analyses/{task_id}/summaries`；SSE 留到后续统一接入。
- 自动回归：后端 `47 passed`，覆盖第二批失败恢复、结果已写但清单未写的崩溃窗口、应用重建、浏览器 Key 不落盘和来源越界拒绝；Ruff、`pip check`、前端 Lint 与生产构建均通过。

#### 5D. 分层汇总 Agent 与最终细纲

**状态：已完成（并入阶段 5 统一验收）**

- 按 Token 预算组合批次概括；超限时建立可恢复的多层汇总树。
- 最终生成总体概述、章节范围细纲、故事线、主要人物关系、世界观、伏笔、冲突和不确定项。
- 汇总节点同样逐个持久化；失败只重跑对应汇总节点。
- 保留来源批次和章节范围，不实现文风与原句摘录。

实现记录：

- 同一个 `run` 后台任务在原文批次全部完成后自动进入分层汇总，并最终转为 `completed`；无需重新创建另一类任务。
- 按完整提示词的实际 Token 数动态组合相邻概括；根节点放不下时生成稳定的多层合并树，每个节点具有稳定 ID、输入指纹和层级。
- 每个中间节点先原子写入独立 JSON，再更新任务清单；网络失败只重试当前节点，进程在“结果已写、清单未写”窗口中断时也不会重复调用该节点。
- 高层提示词只使用直接输入 ID，服务端在校验模型引用后展开为叶子批次 ID 和章节 ID，避免长篇小说因重复携带全部来源 ID 消耗大量上下文；持久结果仍保留完整来源。
- 新增 `hierarchical_outline` Skill、分层汇总 Agent、最终细纲 Agent，以及中间汇总与最终结果的 Pydantic Schema；模型引用当前节点之外的输入会被拒绝。
- 最终细纲包含总体概述、章节范围细纲、故事线、人物及关系变化、世界观、伏笔、未解决事项和冲突/不确定项，不包含文风、原句、生图、仿写或续写字段。
- 新增 `GET /api/analyses/{task_id}/outline`；完整细纲落盘前返回尚未生成，完成后返回结构化结果及供应商、用量和完整来源元数据。
- 自动回归：后端 `50 passed`，覆盖最终细纲接口、汇总失败恢复、汇总结果崩溃窗口、多层合并、来源越界拒绝及浏览器 Key 不落盘；未调用真实模型。

#### 5E. 后端闭环验收

**状态：已验收**

- Mock 完整闭环、动态装箱、超长章节、批次失败恢复、进程重启恢复、SSE 断线、删除与过期清理测试。
- 有效兼容 API 的小样本人工测试只在用户明确愿意产生费用时进行。
- 阶段 5 全部通过后再创建 Git 提交。

实现记录：

- 新增 `GET /api/analyses/{task_id}/events` SSE 接口，从持久任务清单生成精简快照，包含当前阶段、批次计数、汇总节点与层级、最终细纲状态及安全错误。
- SSE 订阅只读取任务状态，不拥有后台任务；关闭订阅流后 Agent 继续运行。`Last-Event-ID` 可避免重连时重复发送相同的当前快照。
- SSE 不返回正文、完整 Checkpoint、API Key 或 Base URL；完成、失败和中断分别使用明确的事件类型。
- 删除上传会先取消对应后台任务，再删除任务目录中的原文、计划、清单、批次概括、汇总节点和最终细纲；过期清理也会回收进程内任务。
- 修复“目录恰好先过期、后台任务同时开始读取”时未回收异步异常的竞态，避免出现 detached task 警告。
- 使用用户提供的十章真实中文小说样本完成 Mock 端到端测试；同时覆盖动态装箱、超长章节拆分、多层汇总、批次/汇总失败恢复、应用重建、SSE 断线、重复启动、删除和过期清理。
- 自动回归：后端 `56 passed`，Ruff、`pip check`、前端 Lint、前端生产构建和 `git diff --check` 全部通过；未调用真实模型。

阶段 5 手动验收步骤：

1. 使用 `start.command` 启动前后端，确认模型配置保持为 Mock，避免产生真实 API 费用。
2. 在页面上传 `tests/fixtures/novels/test_novel.txt`，完成解析并点击“规划分析批次”，记录页面中的任务 ID。
3. 打开 `http://127.0.0.1:8000/docs`，调用 `POST /api/analyses/{task_id}/run`，请求体填写 `{}`，确认返回 `202`。
4. 重复调用 `GET /api/analyses/{task_id}/run`，确认状态依次经过批次概括/汇总并最终成为 `completed`；批次完成数应等于批次数。
5. 调用 `GET /api/analyses/{task_id}/summaries`，确认能够看到按章节范围排列的批次概括。
6. 调用 `GET /api/analyses/{task_id}/outline`，确认返回总体概述、章节范围细纲、故事线、人物、世界观、伏笔和冲突/不确定项。
7. 可在终端执行 `curl -N http://127.0.0.1:8000/api/analyses/<task_id>/events` 查看 SSE；中途按 `Ctrl+C` 只会断开进度订阅，再查询 `run` 时任务应继续或已经完成。
8. 点击页面“立即删除”后，确认上传、`run`、`events` 和 `outline` 接口均不再能读取该任务。
9. 用户确认上述行为后，将阶段 5 状态改为“已验收”并创建阶段 5 Git 提交。

验收记录：用户于 2026-09-03 确认进入下一阶段，阶段 5 已统一验收。

## 阶段 6：细纲工作台与人工修正

**状态：未开始**

范围：

- 串联上传、解析、文本模型配置、批次规划、分析进度和结果查看。
- 分析前展示预计批次数、调用次数和 Token，等待用户确认。
- 展示批次概括、章节范围、总体概述、故事线、人物关系、世界观和伏笔。
- 允许用户修正批次概括和最终细纲。
- 增加加载、空状态、失败重试和取消操作。
- 保持桌面端与窄屏基本可用。

验收标准：

- 从上传到查看 Mock 全书细纲形成完整前端闭环。
- 刷新、取消、失败和空结果均有明确反馈。
- 用户修正后的内容能用于后续导出。

## 阶段 7：DOCX/HTML 细纲导出

**状态：未开始**

范围：

- 选择需要导出的细纲模块。
- 生成带目录、章节范围、故事线、人物关系、世界观和伏笔的 DOCX。
- 生成自包含或资源目录形式的 HTML。
- 对输出文件名和 HTML 内容做安全处理。

验收标准：

- DOCX 可由 Word 正常打开，目录和正文布局可读。
- HTML 可在现代浏览器中离线查看。
- 导出内容与用户在工作台确认的细纲一致。

## 阶段 8：初步交付与解析集中 Debug

**状态：未开始**

范围：

- 补充端到端测试、运行文档和临时数据清理检查。
- 使用真实小说样本集中处理“首版解析已知问题”，补充卷章层级、编号混用、误判和异常排版回归测试。
- 补充长文本批次规划、恢复和分层汇总压力样例。
- 清理所有需求调整后遗留的死代码、旧文案和未使用依赖。

验收标准：

- 结束任务后，上传文件和中间产物按策略清理。
- 已记录的章节解析问题完成统一 Debug，或明确保留为交付版本的已知限制。
- README、PLAN、API 文档和页面只描述当前细纲功能，不残留生图、仿写和文风分析入口。

## 阶段提交策略

- 每个阶段只在用户验收后创建对应 Git 提交。
- 建议提交信息使用 `feat(stage-N): ...` 或 `fix(stage-N): ...`。
- 一个阶段出现问题时，从最近已验收提交排查或回退。
- 新需求先记录到计划，再决定插入当前阶段还是排入后续阶段。
